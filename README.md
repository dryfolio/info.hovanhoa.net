# info.hovanhoa.net

Personal link-in-bio style site for `info.hovanhoa.net`.

## Development

Install dependencies:

```bash
npm install
```

Run the dev server:

```bash
npm run dev
```

Then open `http://localhost:3000` in your browser.

    